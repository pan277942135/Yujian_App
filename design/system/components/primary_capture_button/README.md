# Primary Capture Button

This is the single shared YuJian capture-button contract used by Empty Home V2. The visual core is
solid white with a fine gold rim and deep blue-grey camera icon. Motion and haptic contracts are
in adjacent JSON files. Android uses runtime derivatives mapped from these masters; it does not
make a second Empty Home-specific camera design.
