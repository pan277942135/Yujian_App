# Android Empty Home V2 Asset Map

The Android adapter uses `app/src/main/assets/empty_home_runtime_v2` and the existing Compose
HomeScreen/HomeState architecture. Its inputs are mapped in `asset_map.json`; reference-coordinate
contracts remain platform-neutral and are converted only at draw time.
