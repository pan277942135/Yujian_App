# Empty Home Haptic Spec V1

Camera tap uses the Android light-impact equivalent with a 20 ms semantic hint. Album may use the
platform light-click convention. Page entry, idle environmental motion, bobber motion and ripple
never trigger haptics. The canonical machine-readable source is `shared/contracts/haptic_contract.json`.
