# Recognition Processing AI Ambient Field — Frozen V1

Android implementation source of truth for the Recognition processing visual state runtime.

- Frozen inputs are stored under `design/` and `motion/`.
- The Markdown numeric values override infographic rendering if any discrepancy exists.
- This source controls only visual presentation; detector, crop, classifier and result semantics remain in the existing Recognition runtime contract.

